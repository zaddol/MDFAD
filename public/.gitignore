# working files and temporary screenshots
tmp-*.html
tmp-*.png
tmp-*.py
test-*.png
shot-*.png
strip-*.png
*.tmp

# OS
Thumbs.db
.DS_Store

# this template doesn't use real photos: if examples are needed, add them explicitly
# (never commit personal photos accidentally)
foto/*
!foto/.gitkeep
